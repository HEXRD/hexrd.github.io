import React from 'react';

const GuiSection = () => (
  <section id="gui" style={{ padding: '2rem' }}>
    <h2>HEXRD GUI</h2>
    <p>The HEXRD GUI offers a streamlined interface for...</p>
  </section>
);

export default GuiSection;
